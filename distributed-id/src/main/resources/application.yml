spring:
    application:
        name: distributed-id
    datasource:
        username: root
        password: root
        url: jdbc:mysql://localhost:3306/distributed_id?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
        driver-class-name: com.mysql.cj.jdbc.Driver
